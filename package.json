{
  "name": "narenana-website",
  "version": "0.2.0",
  "private": true,
  "description": "narenana.com landing page — Cloudflare Pages site with embedded routing/cron Function",
  "scripts": {
    "dev": "wrangler pages dev",
    "dev:static": "npx serve site -l 3000",
    "deploy": "wrangler pages deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
